export default function HomePage() {
  return (
    <main>
      <h1>tawf-verify</h1>
      <p>
        A notary, not a custodian. Paste a receipt ID at <code>/verify/&lt;id&gt;</code> to check
        it, or read the full spec in this repository&apos;s <code>prd.md</code>.
      </p>
    </main>
  );
}
